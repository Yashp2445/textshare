import { FileToolsHub } from "@/components/FileToolsHub";

export const metadata = {
  title: "File Tools Hub — Free Client-Side Image, CSV & JSON Utilities | LiveShare",
  description: "Free client-side image compressor & converter (PNG, JPG, WebP), CSV to JSON & JSON to CSV converter, and document exporter running 100% inside your browser.",
};

export default function FileToolsPage() {
  return <FileToolsHub />;
}
