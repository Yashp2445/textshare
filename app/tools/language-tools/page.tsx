import { LanguageToolsHub } from "@/components/LanguageToolsHub";

export const metadata = {
  title: "Programming Language Tools — Formatter, Minifier, Code to Image & Cheat Sheets | LiveShare",
  description: "Free client-side code formatter, minifier, Carbon-style code image creator, and developer cheat sheets for Git, Regex, SQL joins, and VS Code.",
};

export default function LanguageToolsPage() {
  return <LanguageToolsHub />;
}
